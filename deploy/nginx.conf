# ---------------------------------------------------------------------------
# Nginx reverse proxy for the Remote Worker Tracker System
# ---------------------------------------------------------------------------
upstream rwt_app {
    server web:8000;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 30M;

    # Security headers (defence in depth; Django also sets these).
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "same-origin" always;

    # Static assets served directly by Nginx.
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        access_log off;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
        access_log off;
    }

    location /healthz/ {
        proxy_pass http://rwt_app;
        access_log off;
    }

    # Everything else proxies to Gunicorn.
    location / {
        proxy_pass http://rwt_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }
}

# ---------------------------------------------------------------------------
# HTTPS server block (uncomment and provide certificates in production)
# ---------------------------------------------------------------------------
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     # ... same location blocks as above ...
# }
